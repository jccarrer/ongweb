<!DOCTYPE html>

<title>DocumentosVerificacion index</title>

{% block body %}
    <h1>DocumentosVerificacion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Lista_asistencia_url</th>
                <th>Material_entregado_url</th>
                <th>Foto_url</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for documentos_verificacion in documentos_verificacions %}
            <tr>
                <td>{{ documentos_verificacion.id }}</td>
                <td>{{ documentos_verificacion.listaAsistenciaUrl }}</td>
                <td>{{ documentos_verificacion.materialEntregadoUrl }}</td>
                <td>{{ documentos_verificacion.fotoUrl }}</td>
                <td>
                    <a href="{{ path('documentos_verificacion_show', {'id': documentos_verificacion.id}) }}">show</a>
                    <a href="{{ path('documentos_verificacion_edit', {'id': documentos_verificacion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('documentos_verificacion_new') }}">Create new</a>
{% endblock %}
