<form method="post" action="{{ path('resultados_delete', {'id': resultado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resultado.id) }}">
      <button style="margin-top: -73px; margin-left: 188px;" class="btn  btn-primary">Borrar</button>
</form>
