<!DOCTYPE html>

<title>Edit Actividades</title>

{% block body %}
    <h1>Edit Actividades</h1>

    {{ include('actividades/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('actividades_index') }}">back to list</a>

    {{ include('actividades/_delete_form.html.twig') }}
{% endblock %}
