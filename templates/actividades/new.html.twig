<!DOCTYPE html>

<title>New Actividades</title>

{% block body %}
    <h1>Create new Actividades</h1>

    {{ include('actividades/_form.html.twig') }}

    <a href="{{ path('actividades_index') }}">back to list</a>
{% endblock %}
