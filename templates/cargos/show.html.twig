<!DOCTYPE html>

<title>Cargos</title>

{% block body %}
    <h1>Cargos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cargo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cargo.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ cargo.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cargos_index') }}">back to list</a>

    <a href="{{ path('cargos_edit', {'id': cargo.id}) }}">edit</a>

    {{ include('cargos/_delete_form.html.twig') }}
{% endblock %}
